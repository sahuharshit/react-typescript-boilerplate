import { Dropdown as DDropdown } from 'antd';
import styled, { StyledComponent } from 'styled-components';

type DropDownType = typeof DDropdown;

export const DropDown: StyledComponent<DropDownType, any, {}, never> = styled(
  DDropdown
)``;
